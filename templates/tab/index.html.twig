{% extends 'template.html.twig' %}

{% block body %}
<table class="table">
    {% for note in notes %}
    <tr>
        <td>
            {{note}}
        </td>
    </tr>
    {% endfor %}
</table>
<div class="alert alert-info">
    <p>nombre de notes : {{notes | length}}</p>
    <p>note minimal : {{min(notes)}}</p>
    <p>note maximal : {{max(notes)}}</p>
</div>
{% endblock %}